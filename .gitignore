# Credential snapshots and session history now live outside the repo, in
# ~/.config/claude-usage-meter — see README "Security". These two entries stay
# as a backstop for anyone upgrading from the version that kept them in-tree.
accounts/
sessions/
*.tmp-*
.DS_Store
